# 16bit binary to decimal
Zel76 made this as an entry to the rust programming  language

to start:

1. --- open the folder;
2. --- open the src folder;
3. --- open the main.rs;

(you you need to modify the bin array manualy, there is no input just output)*